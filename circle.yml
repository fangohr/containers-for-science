machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - sudo apt-get -q update -y
    - pip install -U pip
    - make install-dependencies

test:
  pre:
  override:
    - make test
