machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - sudo apt-get -q update -y
    - pip install -U pip
    - pip install notebook
    - pip install pytest
    - pip install nbval

test:
  pre:
  override:
    - py.test -v -l -s --nbval .
