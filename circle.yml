machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - sudo apt-get -q update -y
    - pip install -U pip
    - pip install -r requirements.txt

test:
  pre:
  override:
    - make test
